-- return {
--   'oribarilan/lensline.nvim',
--   tag = 'release', -- or: branch = 'release/2.x' for latest non-breaking updates
--   event = 'LspAttach',
--   config = function()
--     require("lensline").setup()
--   end,
-- }
return {}
